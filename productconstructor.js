var Product = function (product_name, product_id, price, category, stock){
    this.product_name = product_name,
    this.product_id = product_id,
    this.price = price,
    this.category = category,
    this.stock = stock
}

module.exports = Product